import java.applet.*;
import java.awt.*;
import java.awt.event.*;
public class exmp extends Applet implements MouseListener
{
   int x,y;
   String msg;
   public void init()
   {
      addMouseListener(this);
   }
   public void paint(Graphics g)
   {
      g.drawString(msg,x,y);
   }
   public void mouseEntered(MouseEvent e)
   {
      showStatus("Mouse has entered");
   }
   public void mouseExited(MouseEvent e)
   {
      showStatus("Mouse has exited");
   }
   public void mousePressed(MouseEvent e)
   {
       x=e.getX();
       y=e.getY();
       msg="Mouse was Pressed";
       repaint();
    }
   public void mouseReleased(MouseEvent e)
   {
       x=e.getX();
       y=e.getY();
       msg="Mouse was released";
       repaint();
    }
   public void mouseClicked(MouseEvent e)
   {
       x=e.getX();
       y=e.getY();
       msg="Mouse was clicked";
       repaint();
    }
 }
HTML Code

<HTML>
<BODY>
<APPLET CODE="exmp.class" HEIGHT=500 WIDTH=500>
</APPLET>
</BODY>
</HTML>